:mod:`katana.units.gzip` --- GZIP files
=========================================================

These units handle procedures that to work with GZIP archive files.

Admittedly, this should be bundled to in a larger, "archive" unit package, but this has not yet been done.

.. toctree::
	gzip/gunzip
